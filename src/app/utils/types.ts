export type NavLink = {
  id: string;
  title: string;
  Path: string;
};

export type NavLinks = NavLink[];

export type fAnswerQuestion = {
  id: string;
  answer: string;
  question: string;
};
export type fAnswerQuestions = fAnswerQuestion[];
