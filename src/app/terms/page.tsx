import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "الأحكام والشروط",
};
const page = () => {
  return <div>page</div>;
};

export default page;
